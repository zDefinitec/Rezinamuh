# Rezinamuh 1.0.0 验收记录

日期：2026-09-18。当前状态：ChatGPT 第二轮复核已批准本地交付，发布审查门槛已通过。技能正文和全部真实模型输出保持冻结。公开发布在批准后另行执行，本记录本身不替代 GitHub 远程状态核验。

## 固定输入与产物

- SKILL.md SHA-256：`3cac750daa22f55b27b3245c37435dd5b33ffcf5b1383bf36eea94f56a1c03af`。
- cases.json SHA-256：`bd3848ce00c15860ece359c66a895c32a703a6f73d33fccbfc1a9065594b35ce`。
- 原始保护区测试文件 SHA-256：`f919c718e04b1d1e3e50c998aaf89cb35a2cb14cee2b7a3d925561656bf8ccde`。
- [model_outputs.jsonl](model_outputs.jsonl) 合并两批真实模型响应，共 40 个案例；原始批记录保留在 outputs-core.jsonl 和 outputs-controls.jsonl。
- 输出记录区分原始响应、终稿、审计信息，包含实际输入、参数、技能哈希和观测时间。T12b 使用 T12a 的实际终稿。

## 已执行检查

- 技能格式校验：PASS。
- 结构检查：PASS。
- 标准库校验脚本单元测试：首轮 55 / 55 PASS；回归补充后最终 65 / 65 PASS。
- 文件副本和测试副本原位修改：已实际执行。两份结果与所记录终稿逐字一致；各自 12 个保护区块不变；原始 fixture 哈希不变。详见 [file-checks.log](runs/file-checks.log)。
- 全量输出机械检查：PASS，命令 `PYTHONDONTWRITEBYTECODE=1 python3 scripts/validate_package.py`，退出码 0。
- 独立语义保真复核：40/40 本轮观测未发现明确语义漂移，技能自身输出约定通过；不是所有冻结测试预期通过，详见 [semantic-review.md](semantic-review.md)。这是模型审查判断，不是自动证明。
- 冻结预期处置：C02-structured 要求同时有标题和标签，实际只有标签，字面预期未满足。技能允许二者择一，因此这是测试设计过度约束，不改技能、不改冻结用例或输出，也不通过其他案例覆盖将它抹掉。
- ChatGPT 独立复核：第一轮确认技能及行为证据可保留，要求修复终稿响应截取校验、区分上述测试预期与语义结论，并清理一处发布说明；第二轮返回 DONE，确认三项问题关闭，批准本地交付并解除发布等待。

真实命令、曾发生的结构检查失败及修复记录见 [execution.log](execution.log)。机械校验不能替代语义审查。

第二轮修正验证：先在旧逻辑下运行新增回归测试，65 项中 7 项确实失败；实现响应完整性检查后，65 项全部通过，结构与 40 条既有输出的完整机械检查均退出 0。普通粘贴/嵌入终稿不能通过截取子串隐藏额外文本；审计、合法冲突披露和文件操作说明的例外仍受各自约束。技能、冻结用例、三个模型输出文件、fixture 和文件产物的 SHA-256 均与修正前一致。

最终批准记录：ChatGPT 读取了第二轮的五份真实执行输出（修复前失败、修复后单元测试、结构检查、完整检查、包内 Git 发布边界），检查六个定向修改文件，并结合首轮完整技能及 40 条响应审查作出结论。批准范围为 Rezinamuh 1.0.0 本地技能包，不是对所有模型、所有冻结测试预期或检测器结果的保证。包内 Git 检查当时有 20 个暂存文件、无历史提交，暂存版本与工作文件一致；因此不存在更早提交泄露未脱敏内容的问题。

## 规则覆盖的实际结果

冻结用例中的映射是执行前的候选计划，不是逐次强制应用清单。4 个原定见证未实现，保留为计划见证 FAIL，不事后修改 cases.json 或输出：

| 规则 | 未实现的原定见证 | 已有实际替代见证 |
| --- | --- | --- |
| 2 重复收尾 | T01 | C01-T01-heavy |
| 4 主题前的铺垫 | R01 | R24 |
| 7 重复句首 | R01 | C01-T06-heavy |
| 20 整齐标题与章节 | C02-structured | C06-on |

合并真实证据后，24 条规则有 apply 观测，规则 21 只有保护性 skip 观测；不能宣称 25 条全部正向应用。逐条证据、其他未命中的候选规则和 C01—C08 控制结果见语义报告。T06 的轻/中强度差异较弱；规则 10 仅演示了表语位置的英文复合形容词，未单独测试前置定语。这些限制保留，不改写为通过所有场景。

## 发布范围检查

公开仓库根目录限定为本技能包，不包含外层私有输入、连接配置或凭据。日志的真实本机路径已替换为明确标注的角色标签，私有输入名称已隐去；仅保留其未改动的完整性摘要。上游来源及 MIT 通知保持完整。独立只读发布审查未发现阻塞项；README 的本地文件链接均可解析。

## 证据边界

这是两个新上下文 Codex 子代理各自共享技能上下文的批量模型烟测，不是每个案例独立会话，不是人类盲审，也不是跨模型验证。精确模型构建号不可获得，记录中不猜测。审查者是另一新上下文模型代理；后续 ChatGPT 复核单独记录。

以下项目未运行（NOT RUN）：人类盲审、跨模型稳定性评估、检测器测试、平台安装兼容性验证。技能不承诺改变任何检测器结果。展示用示例、单元测试中的合成记录和真实模型输出是不同证据，不能互相替代。
